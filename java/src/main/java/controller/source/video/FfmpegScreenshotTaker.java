package controller.source.video;

public class FfmpegScreenshotTaker {

    /*private FFmpegFrameGrabber grabber;
    private Java2DFrameConverter frameConverter;

    public FfmpegScreenshotTaker() throws FrameGrabber.Exception {
        int x = 0, y = 1920, w = 1920, h = 1080;
        grabber = new FFmpegFrameGrabber("desktop");
        grabber.setFormat("gdigrab");
        grabber.setImageWidth(w);
        grabber.setImageHeight(h);
        grabber.setImageMode(FrameGrabber.ImageMode.COLOR);
        grabber.setFrameRate(60);
        grabber.start();
        frameConverter = new Java2DFrameConverter();
    }

    public BufferedImage take() throws FrameGrabber.Exception {
        return frameConverter.convert(grabber.grab());
    }

    public void stop() throws FrameGrabber.Exception {
        grabber.stop();
    }*/

}
